# LiveKit Agent SDK and dependencies
livekit-agents>=0.6.0
livekit>=0.10.0
livekit-plugins-silero>=0.6.0  # VAD (Voice Activity Detection)

# Audio processing
numpy>=1.24.0
soundfile>=0.12.0
scipy>=1.10.0

# Model dependencies (ASR model will have TTS built-in)
torch>=2.0.0
transformers>=4.35.0

# Async support
python-dotenv
asyncio-mqtt

# Logging
colorlog