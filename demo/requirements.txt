# Use GPU-enabled PyTorch for ZeroGPU support
# The base image already has gradio, numpy, pandas, etc.
torch>=2.5.1
torchaudio>=2.5.1
transformers>=4.44.0
peft>=0.6.0
spaces  # Required for ZeroGPU decorator
soundfile  # Required for torchaudio to load audio files