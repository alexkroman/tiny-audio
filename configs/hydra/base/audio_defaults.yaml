# Base audio processing defaults
# These can be overridden in specific configs

# Audio processing
sample_rate: 16000

# Dataset processing
dataset:
  cache_dir: ${hydra:runtime.cwd}/datasets_cache
  max_train_samples: null
  max_eval_samples: 1000
  use_streaming: true