# @package _global_
# Stage 3: Joint fine-tuning of projector + LoRA adapter
# Unfreezes projector to jointly optimize both components
#
# Usage:
#   poetry run python scripts/train.py +experiments=mlp_fine_tune
#
# Requires a pretrained Stage 2 model (projector + LoRA trained)

model:
  projector_type: mlp
  projector_pool_stride: 4
  audio_model_id: zai-org/GLM-ASR-Nano-2512
  text_model_id: Qwen/Qwen3-0.6B

  # LLM LoRA config
  use_lora: true
  lora_rank: 32
  lora_alpha: 64  # Maintain 2.0 scaling factor
  lora_dropout: 0.0
  lora_target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
  freeze_projector: false  # Jointly train projector + LoRA

training:
  hub_model_id: "mazesmazes/tiny-audio"
  learning_rate: 1e-3  # Higher LR to train projector
  warmup_steps: 1000  # Reduced from 2000
  seed: 42  # Different seed for data ordering
  per_device_train_batch_size: 16
  per_device_eval_batch_size: 16
  gradient_accumulation_steps: 1
  num_train_epochs: 1
  save_steps: 2000
  eval_steps: 2000

  use_specaugment: true
  num_time_masks: 2
  time_mask_length: 50  # Reduced from 100 for faster convergence
  num_freq_masks: 2
  freq_mask_length: 27  # Reduced from 43 (~20% of 128 mel bins)
